The brand logo is generated into this folder with:

```bash
swift tool/generate_brand_assets.swift
```

This writes:

- `assets/branding/image.png`
- platform launcher icons for Android, iOS, macOS, web, and Windows
